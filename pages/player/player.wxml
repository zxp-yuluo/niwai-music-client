<!--pages/player/player.wxml-->
    <scroll-view class="player">
        <view style="height: {{navBarHeight + 'px'}};"></view>
        <nw-player-navbar songInfo="{{songInfo}}" />
        <nw-player-info songInfo="{{songInfo}}" />
        
    </scroll-view>